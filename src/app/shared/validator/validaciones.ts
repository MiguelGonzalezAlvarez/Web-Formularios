// import { FormControl } from "@angular/forms";

// Se comenta porque es mas recomendable tener estos elementos
// en un service que en un archivo externo

// export const nombreApellidoPattern: string = '([a-zA-Z]+) ([a-zA-Z]+) ([a-zA-Z]+)';
// export const emailPattern: string = "^[a-z0-9._%+-]+@[a-z0-9.-]+\\.[a-z]{2,4}$";
// export const validacionPersonalizada = (control: FormControl) => {
//     const valor: string = control.value?.trim().toLowerCase();
//     if (valor === 'miguelmuros') {
//         return { validacionPersonalizada: true }
//     }
//     return null;
// }